{% extends 'SanoStaticBundle::layout.html.twig' %}

{% block title %}Symfony - Welcome{% endblock %}

{% block content_header '' %}

{% block content %}
    {% set version = constant('Symfony\\Component\\HttpKernel\\Kernel::MAJOR_VERSION') ~ '.' ~ constant('Symfony\\Component\\HttpKernel\\Kernel::MINOR_VERSION')%}

<div class="vsebina">
    <h1>Komentarji v letu {{ year }} mesec {{ month }}</h1>
    
    {% for item in post %}
        <div id="news">
            <p><span>Ime:</span>{{ item.getName }}</p>
            <p><span>Komentar:</span><br />{{ item.getComment }}</p>
            <p><span>Datum ustvarjanja:</span>{{ item.getCreationDate|date('d.m.Y H:i:s') }}</p>
            <a href="{{ path('_singleComment', ({'id': item.getId})) }}" >Podrobno &raquo;</a>
            <a href="{{ path('_editComment', ({'id': item.getId})) }}" >Uredi</a>
            <a href="{{ path('_deleteComment', ({'id': item.getId})) }}" >Izbriši</a>
        </div>
    {% endfor %}    

</div>
  
{% endblock %}
