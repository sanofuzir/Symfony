{% extends 'SanoStaticBundle::layout.html.twig' %}

{% block title %}Symfony - Welcome{% endblock %}

{% block content_header '' %}

{% block content %}
    {% set version = constant('Symfony\\Component\\HttpKernel\\Kernel::MAJOR_VERSION') ~ '.' ~ constant('Symfony\\Component\\HttpKernel\\Kernel::MINOR_VERSION')%}

      <div class="vsebina">
          
         {% for item in news %}
            <div class="index">
                <h2>{{ item.getTitle }}</h2>
                <p>
                    <strong>{{ item.getSummary }}</strong>
                </p>
                <p>
                    <strong>Datum objave: </strong>{{ item.getPublicationDate|date('d.M.Y') }}
                </p>
                <a class="btn"  href="{{ path('_singleNews', ({'id': item.getId})) }}" >Več &raquo;</a>
            </div>
        {% endfor %}
 
       <div class="index">
            <strong>Ankata</strong>
            <p>Text</p>
            <p><a class="btn" href="#">View details &raquo;</a></p>
       </div>
          
        <div  class="index">
            <strong>Povezave, oglasi</strong>
            <p><a href="{{ path('_admin') }}">Admin Module</a></p>
            <p><a class="btn" href="#">View details &raquo;</a></p>
        </div>
     </div>
      
{% endblock %}
